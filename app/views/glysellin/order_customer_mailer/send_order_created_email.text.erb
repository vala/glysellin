Votre commande a bien été prise en compte et est en attente de paiement

<%= render partial: 'order_header', locals: { order: @order } %>

<%= render partial: 'glysellin/shared/order', locals: { order: @order } %>

<%= render partial: 'order_footer' %>
